"use client";

export default function DevilScene() {
  return (
    <div style={{
      width: "100%",
      height: "100%",
      background: "linear-gradient(to bottom, rgba(139, 0, 0, 0.3), rgba(0, 0, 0, 0.8))",
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
    }}>
      {/* Placeholder for 3D devil scene */}
    </div>
  );
}
